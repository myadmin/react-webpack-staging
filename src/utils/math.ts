export const add = (a: number, b: number) => {
    console.info('I am add func');
    return a + b;
};

export const miuns = (a: number, b: number) => {
    console.info('I am miuns func');
    return a - b;
};
